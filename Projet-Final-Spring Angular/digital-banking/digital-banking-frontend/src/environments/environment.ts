export const environment = {
  production: false,
  // API métier (contrôleurs sous /api)
  backendHost: "http://localhost:8085/api",
  // Endpoints d'authentification (sous /auth)
  authHost: "http://localhost:8085/auth"
};
